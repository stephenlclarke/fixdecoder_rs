[package]
name = "pcap2fix"
version = "0.3.1"
edition = "2021"
license = "AGPL-3.0-only"
description = "PCAP to FIX stream filter: reassembles TCP payloads and emits FIX messages"
repository = "https://github.com/stephenlclarke/fixdecoder_rs"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
pcap-parser = { version = "0.14", features = ["data"] }
etherparse = "0.15"
thiserror = "1.0"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.1"

[build-dependencies]
winresource = "0.1"
